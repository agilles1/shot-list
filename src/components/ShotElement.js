import React from 'react'
import { Button } from 'react-bootstrap'

function ShotElement() {

    return (
        <Button variant="primary" >
            1-1
        </Button>    )
}

export default ShotElement